name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  test-matrix:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v6
        with:
          go-version: "1.23"

      - name: Vet
        shell: bash
        run: CGO_ENABLED=1 go vet -tags sqlite_fts5 ./...

      - name: Test
        shell: bash
        run: CGO_ENABLED=1 go test -tags sqlite_fts5 -race -count=1 ./...

  # Gate job — preserves the "test" check name for branch protection rules.
  test:
    needs: [test-matrix]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Check matrix results
        run: |
          if [ "${{ needs.test-matrix.result }}" != "success" ]; then
            echo "Test matrix failed: ${{ needs.test-matrix.result }}"
            exit 1
          fi

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v6
        with:
          go-version: "1.23"

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.11.3

  build-matrix:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v6
        with:
          go-version: "1.23"

      - name: Build
        shell: bash
        run: CGO_ENABLED=1 go build -tags sqlite_fts5 -ldflags "-s -w" -o bin/mnemonic ./cmd/mnemonic

  # Gate job — preserves the "build" check name for branch protection rules.
  build:
    needs: [build-matrix]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Check matrix results
        run: |
          if [ "${{ needs.build-matrix.result }}" != "success" ]; then
            echo "Build matrix failed: ${{ needs.build-matrix.result }}"
            exit 1
          fi
